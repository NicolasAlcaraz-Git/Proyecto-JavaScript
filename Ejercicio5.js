//definicion de variables
let palabra = "DRAGÓN"                   //se asigna la palabra "DRAGÓN" a la variable palabra

//ingreso de numero
let x = 7                                //se asigna el valor 7 a la variable x

//proceso
for (let i = 0; i < x; i++) {            //for (para) repite i desde 0 mientras sea menor que x
    console.log(palabra);                //i = i + 1; (va sumandole 1 a i)
}